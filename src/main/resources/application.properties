spring.application.name=learn-spring-framework

#postgresql configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/student_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:tuandat29052004}

#hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#jwt configuration
jwt.secret = thisIsmysecregtasdnalskmdalmsfsdnfakdasdalsdasdamsfna
jwt.expiration = 3600000

#account admin 
app.config.adUserName = admin
app.config.adPassword = admin@123

#flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true